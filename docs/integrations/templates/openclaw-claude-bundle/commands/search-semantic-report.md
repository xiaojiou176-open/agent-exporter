---
description: Run semantic retrieval and save a local report artifact
---

Run:

```bash
BeamMe search semantic --workspace-root . --query "$ARGUMENTS" --save-report
```

Then summarize the report path and top hits.
