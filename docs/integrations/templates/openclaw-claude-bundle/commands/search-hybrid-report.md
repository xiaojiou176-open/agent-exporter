---
description: Run hybrid retrieval and save a local report artifact
---

Run:

```bash
AgentExport search hybrid --workspace-root . --query "$ARGUMENTS" --save-report
```

Then summarize the report path and top hits.
